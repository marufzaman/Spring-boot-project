{
  "name": "spring-boot-project",
  "scripts": {
    "springboot": "cd student/target && java -jar ./student-0.0.1-SNAPSHOT.jar",
    "reactjs": "cd reactjs-app-for-student-api && npm install && npm start",
    "concurrent": "concurrently \"npm run springboot\" \"npm run reactjs\"",
    "deploy": "npm install && npm run concurrent"
  },
  "keywords": [],
  "author": "A. M. Almarufuzzaman",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
